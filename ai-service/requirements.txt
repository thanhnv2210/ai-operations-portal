# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Config & validation
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# HTTP client (for Ollama fallback)
httpx==0.28.1

# AI
anthropic==0.42.0

# Observability — LLM tracing (optional: only active when LANGFUSE_* env vars are set)
# Pin to v2.x: server docker image uses langfuse/langfuse:2 which requires SDK v2 API.
# SDK v3+ uses OpenTelemetry export endpoints not present in Langfuse server v2.
langfuse>=2.0.0,<3.0.0

# RAG — vector store + hybrid search
chromadb==0.6.3
rank-bm25==0.2.2

# RAG — embeddings (openai is optional: used when OPENAI_API_KEY is set)
openai==1.82.0

# Utilities
python-multipart==0.0.20

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3
